def stock_scan(Index):
	if Index = 'HS300':
		### Symbol_list = Get list of HS300
		for s in Symbol_list:
			del s if s.match('ST')
	elif Index = 'A':
		### Symbol_list = Get list of HS300
		for s in Symbol_list:
			del s if s.match('ST')
	return Symbol_list