class Order(object):
	def __init__(self, symbol, direction, amount, tranact_price,state = Normal)
		self.symbol = symbol
		self.direction = direction
		self.amount = amount
		self.tranact_price = tranact_price
		self.state = state
	